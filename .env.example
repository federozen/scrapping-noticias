# Copiá este archivo a ".env" para probar localmente (no se sube a git).
# En Render estas mismas dos variables se configuran desde el dashboard,
# nunca acá.
BASIC_AUTH_USER=facu
BASIC_AUTH_PASS=cambiame-por-una-contrasena-real
